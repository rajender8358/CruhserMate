<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/splash_status_bar</item>
        <item name="android:navigationBarColor">@color/splash_navigation_bar</item>
    </style>

    <!-- Colors for splash screen -->
    <color name="splash_status_bar">#FFFFFF</color>
    <color name="splash_navigation_bar">#FFFFFF</color>
    <color name="splash_background">#FFFFFF</color>

</resources>
